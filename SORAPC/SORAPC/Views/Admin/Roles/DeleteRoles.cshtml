@model SORAPC_API.Models.Role
@{
    ViewData["Title"] = "Удалить роль";
}

<div class="container mt-5 pt-5">
    <h2 class="text-center mb-4">Удалить роль</h2>
    <div class="card shadow p-4">
        <h3 class="text-danger text-center">Вы уверены, что хотите удалить эту роль?</h3>
        <dl class="row">
            <dt class="col-sm-3">ID</dt>
            <dd class="col-sm-9">@Model.IdRole</dd>
            <dt class="col-sm-3">Название</dt>
            <dd class="col-sm-9">
                @{
                    string roleName = Model.IdRole == 1 ? "Администратор" :
                    Model.IdRole == 2 ? "Клиент" :
                    Model.Title ?? "Неизвестная роль";
                    <span class="badge @(Model.IdRole == 1 ? "bg-primary" : "bg-secondary")">@roleName</span>
                }
            </dd>
        </dl>
        <form asp-action="DeleteRoles" asp-controller="Admin" method="post">
            <input type="hidden" asp-for="IdRole" />
            <div class="text-center mt-4">
                <button type="submit" class="btn btn-danger">Удалить</button>
                <a asp-action="Roles" class="btn btn-secondary">Назад</a>
            </div>
        </form>
    </div>
</div>