<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm fixed-top">
        <div class="container-fluid">
            <!-- Логотип -->
            <a class="navbar-brand fw-bold text-white" asp-controller="Home" asp-action="Index">
                <i class="fas fa-desktop me-2"></i> SORAPC
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
                    aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Навигация и иконки -->
            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" asp-controller="Catalog" asp-action="Catalog">Каталог</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" asp-controller="Services" asp-action="Services">Услуги</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" asp-controller="Support" asp-action="Support">Поддержка</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white fw-semibold" asp-controller="Company" asp-action="Company">Компания</a>
                    </li>
                </ul>

                <!-- Поиск и иконки -->
                <div class="d-flex align-items-center">
                    <!-- Поиск -->
                    <div class="search-bar position-relative me-3">
                        <input type="text" class="form-control rounded-pill" placeholder="Поиск по сайту" style="padding-right: 2.5rem;">
                        <span class="position-absolute" style="right: 10px; top: 50%; transform: translateY(-50%);">
                            <i class="fas fa-search text-muted"></i>
                        </span>
                    </div>
                    <!-- Профиль -->
                    <a asp-controller="Auth" asp-action="@(User.Identity.IsAuthenticated ? "Profile" : "Login")" class="text-white me-3">
                        <i class="fas fa-user-circle fa-lg"></i>
                    </a>
                    <!-- Корзина -->
                    <a asp-controller="Cart" asp-action="Cart" class="text-white me-3">
                        <i class="fas fa-shopping-cart fa-lg"></i>
                    </a>
                </div>
            </div>
        </div>
    </nav>
</header>

<style>
    .navbar {
        transition: background-color 0.3s ease;
    }

    .navbar-brand:hover, .nav-link:hover {
        color: #00d4ff !important;
    }

    .search-bar .form-control {
        border: none;
        background: #ffffff1a;
        color: #fff;
    }

        .search-bar .form-control::placeholder {
            color: #ccc;
        }

        .search-bar .form-control:focus {
            background: #ffffff33;
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
        }

    .navbar-dark .nav-link {
        transition: color 0.3s ease;
    }
</style>